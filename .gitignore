.env
# Bundle de despliegue: contiene deploy/.env (secreto), el front-controller
# de producción y los .sql de migración. Se documenta en docs/operaciones/.
deploy/
users.txt
database.txt

# Artefactos comprimidos (nunca se commitean)
*.zip
/plataforma.zip

vendor/
writable/logs/
writable/cache/
writable/session/
writable/debugbar/
writable/uploads/
# Scripts operativos con credenciales locales (los versionados, sin secretos,
# están en docs/operaciones/scripts/)
writable/*.sh
.claude/
nul
app/Database/Database/
public/uploads/
build/

# scripts locales con credenciales
writable/*.sh

# JS de tests (jsdom) — se instala con: npm --prefix tests/js install
node_modules/
tests/js/package-lock.json
